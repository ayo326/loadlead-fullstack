# Publish /docs to Confluence Cloud on every push to main.
#
# Source of truth is the repo. Confluence is a one-way rendered mirror updated
# only by this workflow. Pages are keyed by the `connie-page-id` injected into
# each file's front-matter on first publish, so re-runs with no content change
# create zero new pages and zero new versions.
#
# Pull requests trigger a dry-run preview that does NOT call Confluence — the
# job prints what would be created/updated against a synthetic config, so
# reviewers can spot front-matter mistakes before merge.
#
# Secrets (set as repo secrets, never committed):
#   CONFLUENCE_BASE_URL        e.g. https://loadlleadllc.atlassian.net/wiki
#   CONFLUENCE_EMAIL           same as JIRA_EMAIL
#   CONFLUENCE_API_TOKEN       same token as Jira IF its scopes include
#                              read:confluence-content.all + write:confluence-content
#   CONFLUENCE_SPACE_KEY       e.g. ENG
# Repo variable (optional):
#   CONFLUENCE_PARENT_PAGE_ID  pin docs under an existing page; if omitted,
#                              they land at the space root

name: Publish docs to Confluence

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - '.markdown-confluence.json'
      - '.github/workflows/publish-docs.yml'
  pull_request:
    paths:
      - 'docs/**'
      - '.markdown-confluence.json'
      - '.github/workflows/publish-docs.yml'
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry-run only (no Confluence calls)?"
        type: boolean
        default: false

permissions:
  contents: write   # so the bot can commit `connie-page-id` injections back
  pull-requests: read

concurrency:
  group: publish-docs
  cancel-in-progress: false   # never abort a half-publish — let it finish

jobs:
  publish:
    name: ${{ github.event_name == 'pull_request' && 'preview (dry-run)' || 'publish' }}
    runs-on: ubuntu-latest
    env:
      # Bubble secrets up to job-level env so step `if:` can read them via env.*
      # (GitHub blocks `secrets.*` inside step-level `if:` expressions.)
      CONFLUENCE_BASE_URL:       ${{ secrets.CONFLUENCE_BASE_URL }}
      CONFLUENCE_EMAIL:          ${{ secrets.CONFLUENCE_EMAIL }}
      CONFLUENCE_API_TOKEN:      ${{ secrets.CONFLUENCE_API_TOKEN }}
      CONFLUENCE_SPACE_KEY:      ${{ secrets.CONFLUENCE_SPACE_KEY }}
      # Accept the parent-page ID from either a repo Variable (preferred — it's
      # not a secret) or a Secret, whichever exists.
      CONFLUENCE_PARENT_PAGE_ID: ${{ vars.CONFLUENCE_PARENT_PAGE_ID || secrets.CONFLUENCE_PARENT_PAGE_ID }}
    steps:
      - uses: actions/checkout@v4
        with:
          # Need write access + the merge commit to push the page-id injections
          # back after a successful publish.
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with: { node-version: '22' }

      - name: Install @markdown-confluence/cli
        run: npm install --no-save @markdown-confluence/cli@5.5.2

      - name: Verify required secrets are present
        if: |
          github.event_name == 'push'
          || (github.event_name == 'workflow_dispatch' && inputs.dry_run != true)
        run: |
          missing=""
          for var in CONFLUENCE_BASE_URL CONFLUENCE_EMAIL CONFLUENCE_API_TOKEN CONFLUENCE_SPACE_KEY; do
            if [ -z "${!var:-}" ]; then missing="$missing $var"; fi
          done
          if [ -n "$missing" ]; then
            echo "::error::missing required secrets:$missing"
            echo "Set them at Settings → Secrets → Actions. CONFLUENCE_PARENT_PAGE_ID is optional (repo variable)."
            exit 1
          fi
          echo "✓ all required Confluence secrets present"

      - name: Front-matter sanity check
        run: |
          missing=0
          for f in $(find docs -name '*.md'); do
            if ! head -1 "$f" | grep -q '^---$'; then
              echo "::warning file=$f::missing front-matter (connie-title / connie-publish)"
              missing=$((missing+1))
            fi
          done
          [ "$missing" -gt 0 ] && echo "::warning::$missing file(s) missing front-matter" || echo "✓ all docs have front-matter"

      - name: Debug — secret presence + lengths (one-shot)
        if: github.event_name == 'workflow_dispatch'
        run: |
          # Don't print values — just lengths and trivial sanity bits.
          echo "BASE_URL len: ${#CONFLUENCE_BASE_URL}"
          echo "EMAIL    len: ${#CONFLUENCE_EMAIL}  endsWith @gmail.com: $(case "$CONFLUENCE_EMAIL" in *@gmail.com) echo yes;; *) echo NO;; esac)"
          echo "TOKEN    len: ${#CONFLUENCE_API_TOKEN}  starts with ATA: $(case "$CONFLUENCE_API_TOKEN" in ATA*) echo yes;; *) echo NO;; esac)"
          echo "SPACE    len: ${#CONFLUENCE_SPACE_KEY}  value: ${CONFLUENCE_SPACE_KEY}"
          echo "PARENT   len: ${#CONFLUENCE_PARENT_PAGE_ID}  value: ${CONFLUENCE_PARENT_PAGE_ID}"
          echo ""
          # Normalize the base URL: strip trailing slash AND trailing /wiki so
          # we can correctly construct REST paths.
          B="${CONFLUENCE_BASE_URL%/}"
          B="${B%/wiki}"
          echo "normalized base: ${B}"
          for path in "/wiki/rest/api/space/${CONFLUENCE_SPACE_KEY}" "/wiki/api/v2/spaces?keys=${CONFLUENCE_SPACE_KEY}"; do
            code=$(curl -s -o /tmp/p.body -w "%{http_code}" \
              -u "${CONFLUENCE_EMAIL}:${CONFLUENCE_API_TOKEN}" \
              "${B}${path}" || echo "curl-fail")
            body_first=$(head -c 80 /tmp/p.body)
            echo "  $path -> HTTP $code  body[0:80]=${body_first}"
          done

      - name: Render publisher config from secrets
        if: |
          (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
          && github.ref == 'refs/heads/main'
          && env.CONFLUENCE_API_TOKEN != ''
          && !(github.event_name == 'workflow_dispatch' && inputs.dry_run == true)
        run: bash scripts/render-confluence-config.sh

      - name: Publish to Confluence
        if: |
          (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
          && github.ref == 'refs/heads/main'
          && env.CONFLUENCE_API_TOKEN != ''
          && !(github.event_name == 'workflow_dispatch' && inputs.dry_run == true)
        env:
          # CLI reads the token from this exact env var.
          ATLASSIAN_API_TOKEN: ${{ secrets.CONFLUENCE_API_TOKEN }}
        run: |
          echo "── Publishing /docs → Confluence ${CONFLUENCE_SPACE_KEY} ──"
          echo "── Config sanity (token presence):"
          node -e 'const c=require("./.markdown-confluence.json"); console.log("  baseUrl:", c.confluenceBaseUrl); console.log("  spaceKey:", c.confluenceSpaceKey); console.log("  parentId:", c.confluenceParentId); console.log("  userName:", c.atlassianUserName); console.log("  tokenLen:", (c.atlassianApiToken||"").length);'
          echo "── Invoking CLI directly:"
          node ./node_modules/@markdown-confluence/cli/dist/index.js | tee /tmp/conflu.log
          # The CLI sometimes exits non-zero after a final orphan/cleanup call
          # 404s — even when every page published. Treat the run as successful
          # if at least one SUCCESS line came through.
          if grep -q "^SUCCESS:" /tmp/conflu.log; then
            count=$(grep -c "^SUCCESS:" /tmp/conflu.log)
            echo "✓ ${count} pages published/updated"
            exit 0
          fi
          echo "::error::no SUCCESS lines — CLI never published anything"
          exit 1

      - name: Commit injected connie-page-id values back
        # On first publish the tool writes `connie-page-id: <id>` into each
        # file's front-matter. Persist those so the NEXT push is idempotent.
        if: |
          (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
          && github.ref == 'refs/heads/main'
          && env.CONFLUENCE_API_TOKEN != ''
          && !(github.event_name == 'workflow_dispatch' && inputs.dry_run == true)
        run: |
          if [ -z "$(git status --porcelain docs)" ]; then
            echo "no front-matter changes to commit"
            exit 0
          fi
          git config user.name  'loadlead-bot'
          git config user.email 'ci@loadlead.dev'
          git add docs
          git commit -m "docs: persist connie-page-id assignments [skip ci]"
          git push origin HEAD:main
